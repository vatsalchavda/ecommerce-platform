server:
  port: 7070

spring:
  application:
    name: product-service
  data:
    mongodb:
      host: localhost
      port: 27018
      database: product-db

# Swagger/OpenAPI Configuration
springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html

# Logging
logging:
  level:
    root: INFO
    com.ecommerce: DEBUG

